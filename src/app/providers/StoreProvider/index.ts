import { StoreProvider } from "./ui/StoreProvider"
import { createReduxStore } from "./config/store"
import type { StateSchema , ThunkExtraArgument} from "./config/stateSchema"

export  {
    StoreProvider,
    createReduxStore,
    StateSchema,
    ThunkExtraArgument
}
